import React, { useState, useEffect } from react; import axios from axios; import config from ./config; function App() { const [data, setData] = useState(null); useEffect(() => { axios.get(config.mcp.url, { headers: config.mcp.headers }) .then(res => setData(res.data)) .catch(err => console.error(err)); }, []); return ( <div className=App> <h1>MCP API Response</h1> {data ? <pre>{JSON.stringify(data, null, 2)}</pre> : <p>Loading...</p>} </div> ); export default App;
